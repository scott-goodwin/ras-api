{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type" : "object",
  "properties":
  {
    "currentYearResidencyStatus"  :
    {
      "description": "Returns the residency status for this tax year. This will be returned all year round.",
      "$ref" : "#/definitions/UKResidencyStatusType",
      "example": "scotResident"
    },
    "nextYearForecastResidencyStatus"  :
    {
      "description": "Returns the residency status for the next tax year. This will be returned from 1st January up to and including 5th April, each year.",
      "$ref" : "#/definitions/UKResidencyStatusType",
      "example": "otherUKResident"
    }
  },
  "required" :
  [
    "currentYearResidencyStatus"
  ],
  "definitions" :
  {
    "UKResidencyStatusType" :
    {
      "type" : "string",
      "enum" :
      [
        "scotResident", "otherUKResident"
      ]
    }
  }
}
